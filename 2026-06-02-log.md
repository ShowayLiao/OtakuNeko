# 2026-06-02 执行日志

## 任务概述

前端 Chat 流式渲染问题诊断：发送请求后，全部内容被隐藏在「正在思考」标签下方无法展开查看，直至输出结束。

---

## 核心文件变动

| 文件 | 操作 | 说明 |
|------|------|------|
| `docs/reports/chat-rendering-blocked-by-thinking-2026-06-02.md` | **新建** | 诊断报告，含 3 个根因 + 2 个修复方案 |

**无源代码修改** — 本次为纯诊断阶段（Phase 1），待用户确认方案后进入执行阶段。

---

## 根因摘要

1. **主因** — [ProcessStepItem.tsx](file:///e:/HACCI/Documents/tools/OtakuNeko/frontend/src/components/chat/ProcessStepItem.tsx#L31-L44)：`isPending` 为 `true` 时禁用点击展开，思考阶段内容完全不可见。
2. **次因** — [FinalAnswerBlock.tsx](file:///e:/HACCI/Documents/tools/OtakuNeko/frontend/src/components/chat/FinalAnswerBlock.tsx#L12-L13)：`!hasContent` 时无条件返回 `null`，`isStreaming` 参数形同虚设。
3. **加剧** — [ProcessContainer.tsx](file:///e:/HACCI/Documents/tools/OtakuNeko/frontend/src/components/chat/ProcessContainer.tsx#L78-L82)：流式开始自动展开，配合根因 1 造成"所有内容被吃掉"的用户感受。

---

## 推荐修复方案（方案 A：最小侵入）

- A1: 移除 `ProcessStepItem` 中 `isPending` 对点击展开的阻止
- A2: 移除 `FinalAnswerBlock` 中 `!hasContent` 返回 `null` 的逻辑
- A3: `MessageList` 中 `{msg.content && ...}` 改为 `{(msg.content || isStreaming) && ...}`

---

## 后续待办

- [ ] 等待用户确认方案（A 最小修复 / B 激进简化）
- [ ] 实施代码修改
- [ ] 编写组件测试 `ProcessStepItem.test.tsx`（同级目录就近原则）
- [ ] 同步更新相关文档

---

## 测试指令

```bash
# 前端组件测试（待编写）
cd frontend && npx vitest run src/components/chat/ProcessStepItem.test.tsx
```

---

---

## 任务 2：Think-Speak 双节点架构改造计划

### 背景

当前 `graph.py` 通过流式解析 `<think>...</think>` XML 标签区分推理与输出。存在以下结构性问题：
- System Prompt 未指令标签格式，非 DeepSeek-R1 模型不工作
- 8 字符滚动 buffer 有边界漏洞
- 标签与用户讨论内容冲突
- 标签本身消耗 token

### 决策

采用**方案 C（双节点物理分离）**：利用 LangGraph `astream_events` v2 的 `metadata.langgraph_node` 字段实现 think → speak 节点级流式分流。

### 核心文件变动

| 文件 | 操作 | 说明 |
|------|------|------|
| `docs/plans/think-speak-dual-node-refactor.md` | **新建 → 修订** | 完整改造计划（9 步实现含 reasoning_trace API + interrupt 思考中断 + 测试策略） |

### 计划摘要

- **图拓扑变更**：`START → think → [tools?] → tools → think` 循环，推理完毕路由到 `speak → END`（可选 `interrupt_before=["speak"]`）
- **节点职责**：think 用 `llm.bind_tools()` + 固定推理 prompt；speak 用 `llm`（无工具）+ 用户 persona prompt
- **流式分流**：`node_name == "think"` → `thinking_*` SSE；`node_name == "speak"` → `message_*` SSE
- **reasoning_trace 持久化**：think 结束时 SSE 推送完整推理 + `GET /chat/reasoning/{id}` API
- **思考中断**：LangGraph `interrupt_before=["speak"]` 暂停等待审核 + `POST /chat/resume` 端点
- **前端改动**：核心 SSE 事件不变，新增 `reasoning_trace` 事件监听 + interrupt 模式下 resume 交互
- **代价**：每次对话多一次 LLM 调用（think → speak）

### 后续待办

- [ ] 等待用户确认计划
- [ ] 实施 Phase 2：按计划 9 步依次修改 `graph.py` → `agent.py` → `schemas/agent.py` → `README.md`
- [ ] 编写 `tests/agents/test_graph_dual_node.py`
- [ ] 运行测试验证

### 测试指令（待编写）

```bash
cd backend
uv run pytest tests/agents/test_graph_dual_node.py -v
```
