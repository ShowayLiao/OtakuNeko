# 2026-05-25 执行日志

## 任务概述
诊断并修复 OtakuNeko 聊天流式渲染异常：前端始终显示「正在思考」、无逐字流式效果、正文内容错误落入推理面板。

## 调研范围

### 前端文件（全部已审查）
| 文件 | 用途 |
|------|------|
| `frontend/src/hooks/useChatStreaming.ts` | 流式 SSE 事件回调处理 |
| `frontend/src/components/chat/MessageList.tsx` | 消息列表渲染 |
| `frontend/src/components/chat/AgentMessageRenderer.tsx` | Agent 状态机 + 推理/工具/正文渲染 |
| `frontend/src/components/chat/index.tsx` | 聊天页主入口 |
| `frontend/src/stores/useChatStore.ts` | Zustand 消息状态管理 |
| `frontend/src/lib/fetcher.ts` | SSE 解析 + fetch 封装 |

### 后端文件（全部已审查）
| 文件 | 用途 |
|------|------|
| `backend/app/agents/graph.py` | LangGraph 工作流 + 流式 SSE 相位机 |
| `backend/app/api/v1/agent.py` | FastAPI SSE 端点 |
| `backend/app/schemas/agent.py` | 流式事件 schema 定义 |

## 根因

**文件**: `backend/app/agents/graph.py` 第 228-230 行  
**问题**: 流式相位机的默认路径设为 `THINKING`，导致所有不使用 `` 标签的模型（OpenAI、Claude、Gemini、DeepSeek V3 等）的输出 token 全部被归类为 `thinking_chunk`，而非 `message_chunk`。

## 修复

**文件**: `backend/app/agents/graph.py`
**变更**: 第 228-230 行，将默认相位从 `THINKING` 改为 `FINAL_MESSAGE`
```diff
 if phase == _StreamPhase.IDLE:
-    phase = _StreamPhase.THINKING
-    yield _emit("thinking_start")
+    phase = _StreamPhase.FINAL_MESSAGE
```

**影响范围**: 仅 2 行删除。
**风险**: 低 — 不改变 `` 标签检测逻辑，仅修改无标签时的默认路径。
**前端改动**: 无需修改。

## 文档同步

- ✅ 生成诊断报告: `docs/reports/streaming-rendering-diagnosis-2026-05-25.md`
- ✅ 后端 `app/agents/README.md` 无需更新（不影响 API 契约）

## 验证指令

```bash
# 重启后端
cd backend && uv run uvicorn app.main:app --reload

# 前端
cd frontend && npm run dev
```

---

## 第二阶段：前端「思考与工具调用」流式渲染重构

**依据**: `docs/reports/streaming-process-visualization-gap-analysis.md`

### 需求对照报告
- ✅ 已生成: `docs/reports/streaming-process-visualization-gap-analysis.md`

### 数据模型重构
**文件**: `frontend/src/stores/useChatStore.ts`
- ✅ 新增 `ProcessNode` 接口（id, stepNumber, type, status, title, duration, details, output, name, reason）
- ✅ 新增 `MessageStatus` 类型（'thinking' | 'generating' | 'completed' | 'error'）
- ✅ `Message` 接口新增 `processes?: ProcessNode[]` 和 `status?: MessageStatus`
- ✅ `updateMessage` 签名简化：`(sessionId, content, messageId?, processes?, plan?, status?)`
- ✅ 移除分散的 toolCallUpdate/reasoningText/progressSteps 参数

### 新增组件
| 文件 | 说明 |
|------|------|
| `ProcessStepItem.tsx` | 统一渲染 thought/tool_call 节点，pending/success/error 三态 |
| `ProcessContainer.tsx` | 展开/折叠动画（grid-template-rows transition）+ 锚点滚动 |
| `FinalAnswerBlock.tsx` | 最终回复区域，与过程容器物理隔离 |

### 流式处理重构
**文件**: `frontend/src/hooks/useChatStreaming.ts`
- ✅ 所有 SSE 回调改为构建 `processes[]` 统一数组
- ✅ 步骤序号（stepNumber）由 `stepCounterRef` 驱动
- ✅ 推理/工具调用节点统一追加，status 实时流转（pending→success）
- ✅ `requestAnimationFrame` 节流：高频 token 更新合并为每帧一次 flush
- ✅ 工具调用失败标记所有 pending 节点为 error

### 组件简化
**文件**: `frontend/src/components/chat/AgentMessageRenderer.tsx` (451→63 行)
- ✅ 移除内联 ToolCallCard / 推理块 / 展开逻辑
- ✅ 组合 ProcessContainer + FinalAnswerBlock
- ✅ `deriveStatus` 签名更新为 `(hasContent, processes[], isStreaming)`

### 展开/折叠行为修正（P0）
- ✅ 流开始时**自动展开**（`setExpanded(true)`，之前反了）
- ✅ 流结束且所有节点完成时**自动折叠**（`!isStreaming && allDone`）
- ✅ 用户手动展开后不再自动折叠（`userToggledRef`）

### MessageList 适配
- ✅ 传递 `processes` 替代 `toolCalls + reasoningText + progressSteps`
- ✅ `isStreamingPlaceholder` 检查改为 `processes?.length`

### 测试文件同步
- ✅ `AgentMessageRenderer.test.tsx` 更新为新 API（ProcessNode fixtures）

### TypeScript 诊断
- ✅ 全部 7 个修改/新增文件零错误

---

## 后续待办
- [ ] 用户在实际环境中验证 OpenAI / DeepSeek V3 / Claude 模型的流式渲染
- [ ] 验证 DeepSeek R1 的 reasoning 模式未受回归影响
- [ ] 验证前端展开/折叠动画 + 锚点滚动
- [ ] pnpm install 安装 vitest 后运行测试套件
