# 2026-05-11 执行日志

## 执行时间
2026-05-11

## 任务简述
执行 `docs/plans/agent-implementation-roadmap.md` 中的 **P1-01：JWT 密钥与环境变量安全修复**

## 核心文件变动

| 文件 | 操作 | 说明 |
|------|------|------|
| `backend/app/core/config.py` | 修改 | 新增 `JWT_SECRET_KEY`（空字符串默认值，强制校验）和 `CORS_ORIGINS`（逗号分隔）两字段 |
| `backend/app/core/security.py` | 修改 | `SECRET_KEY` 改为从 `settings.JWT_SECRET_KEY` 读取，空值时抛 `ValueError`；`decode_access_token` 中 debug 日志从 `logging.info` 降级为 `logging.debug` |
| `backend/app/main.py` | 修改 | CORS `allow_origins` 从硬编码数组改为从 `settings.CORS_ORIGINS` 动态解析 |
| `backend/.env.example` | 修改 | 新增 `JWT_SECRET_KEY=dev-jwt-secret-change-in-production` 及生成说明 |
| `backend/.env` | 修改 | 同步补充 `JWT_SECRET_KEY`（运行时读取的配置文件，原计划遗漏） |

## 文档同步记录
- `backend/app/core/README.md`：更新 `config.py` 字段列表（新增 JWT_SECRET_KEY、CORS_ORIGINS）和 `security.py` 密钥来源说明（从 OPENAI_API_KEY fallback 改为 JWT_SECRET_KEY 强制校验）

## 测试验证

```powershell
# 在宿主环境执行
cd e:\HACCI\Documents\tools\OtakuNeko\backend
uv run uvicorn app.main:app --reload
```

预期：服务正常启动，无 `ValueError`。

## 后续待办
- P1-02：密码强度校验
- P1-03：日志敏感信息脱敏
- 数据库 Subject 表脏数据清理（name 为空记录）

---

## 任务简述
执行 Timetable 页面性能优化 Tier 1（低成本高收益），详见计划文档 `docs/plans/timetable-performance-optimization.md`。

## 核心文件变动

| 文件 | 操作 | 说明 |
|------|------|------|
| `frontend/src/components/timetable/TimelineMediaCard.tsx` | 修改 | 包裹 `React.memo` + 自定义 `arePropsEqual` 比较函数，跳过无关 props 变化的重渲染 |
| `frontend/src/components/timetable/DraggableItemWrapper.tsx` | 修改 | 包裹 `React.memo` + 自定义 `arePropsEqual`，基于 `id` 和 `data.source/source_id` 比较 |
| `frontend/src/app/Timetable/page.tsx` | 修改 | 9 个回调函数从普通函数改为 `useCallback` 包裹（含 `handleFetchBangumiCalendar`、`handleClearAll`、`openTickTickModal` 等），消除内联匿名函数 |
| `frontend/src/app/Timetable/page.tsx` | 修改 | `CollectionPanel` 从静态 import 改为 `next/dynamic` 懒加载（`ssr: false`），首屏减少 ~40% JS 体积 |
| `frontend/src/services/scheduleService.ts` | 修改 | 移除 13 处 verbose `console.log`（含 map 循环内日志），仅保留 `console.error` |
| `frontend/src/components/timetable/DraggablePanel.tsx` | 修改 | 移除 2 处 `console.log` |

## 文档同步记录
- `docs/plans/timetable-performance-optimization.md`：新建，含完整诊断、Tier 1-3 方案、验证方式

## 测试验证

```powershell
# TypeScript 类型检查
cd e:\HACCI\Documents\tools\OtakuNeko\frontend
pnpm typecheck
```

所有 type 错误均为预先存在的（chat/index.tsx、SearchBar.tsx、RoleImportModal.tsx、fetcher.ts），本次修改零新增错误。

```powershell
# 启动前端验证渲染
cd e:\HACCI\Documents\tools\OtakuNeko\frontend
pnpm dev
```

预期：Timetable 页面正常渲染，拖拽卡片流畅不卡顿，CollectionPanel 按需加载。

## 后续待办
- Tier 2：TimelineBoard / StandardLanes 包裹 React.memo；itemsBySlotMap 归一化预处理拆分
- Tier 3：CSS Container Queries 替代 ResizeObserver；useTransition 隔离拖拽状态；虚拟滚动

---

## 任务简述
修复 `start_all.bat` 中 `pnpm install` 静默失败后仍继续执行，导致 `next dev` 命令找不到的连锁错误。

## 根因分析
- `pnpm install` 执行后未检查 `%errorlevel%`，即使安装失败也打印 `[OK]` 并继续
- 新机通过 `corepack enable` 安装 pnpm 后，未二次验证 pnpm 是否真正可用
- 导致 Stage [4/4] 时 `next` CLI 不在 `node_modules/.bin` 中，抛 `'next' is not recognized`

## 核心文件变动

| 文件 | 操作 | 说明 |
|------|------|------|
| `start_all.bat` | 修改 | Step 0：corepack 安装 pnpm 后新增 `where pnpm` 二次验证，失败则 `exit /b 1` 阻断 |
| `start_all.bat` | 修改 | Step 2：`pnpm install` 后新增 `%errorlevel%` 检查，失败则 `exit /b 1` 阻断 |

## 文档同步记录
- 无关联局部文档需更新（启动脚本为根目录基础设施）

## 测试验证

```powershell
# 在宿主环境模拟新机前端依赖场景
cd e:\HACCI\Documents\tools\OtakuNeko
.\start_all.bat
```

预期：pnpm 安装/验证失败时脚本立即中止并提示错误，不再盲目前进到 `pnpm dev`。

---

## 任务简述
修复新电脑 `start_all.bat` 一键启动脚本导致的 PostgreSQL 连接失败错误。

## 根因分析
`start_all.bat` 在首次运行时若 `.env` 不存在，会自动从 `.env.example` 复制。但 `.env.example` 默认 `DEPLOY_MODE=cloud`，导致 SQLAlchemy 尝试连接 `localhost:5432` 的 PostgreSQL（新电脑上不存在），启动失败并抛出 `ConnectionDoesNotExistError`。

而 `config.py` 中 `Settings.DEPLOY_MODE` 的代码默认值已是 `"local"`，与 `.env.example` 不一致，属于配置漂移。

## 核心文件变动

| 文件 | 操作 | 说明 |
|------|------|------|
| `backend/.env.example` | 修改 | `DEPLOY_MODE=cloud` → `DEPLOY_MODE=local`，与代码默认值对齐，新机首次运行默认使用 SQLite |

## 文档同步记录
- `backend/app/core/README.md`：无需更新（已正确描述 local 为默认模式）
- `backend/QUICK_START.md`：无需更新（已正确推荐本地模式为开发默认）

## 测试验证

```powershell
# 模拟新机场景：删除 .env，重新运行脚本
cd e:\HACCI\Documents\tools\OtakuNeko
del backend\.env
.\start_all.bat
```

预期：后端以 local/SQLite 模式启动，无 PostgreSQL 连接错误。

## 后续待办
- 可考虑在 `start_all.bat` 中增加交互询问（本地模式 / 云端模式），让用户首次明确选择

---

## 任务简述
为上述修复配套，创建脏数据清理脚本 `backend/scripts/cleanup_empty_subjects.py`。

## 核心文件变动

| 文件 | 操作 | 说明 |
|------|------|------|
| `backend/scripts/cleanup_empty_subjects.py` | 新建 | 两步式清理脚本：默认 inspect 模式预览脏数据，`delete` 参数确认后删除 name 为空的 Subject 记录 |

## 使用方式

```powershell
# 1. 先预览（安全，只读）
cd e:\HACCI\Documents\tools\OtakuNeko\backend
uv run python scripts/cleanup_empty_subjects.py

# 2. 确认后执行删除
uv run python scripts/cleanup_empty_subjects.py delete
```

## 后续待办
- P1-03：日志敏感信息脱敏

---

## 任务简述（v2 修复）
前端仍渲染空数据卡片。根因：collection_repo 仅将 subject 置 `None`，但 `collection_with_subject_list_to_unified_list()` 不做过滤，`{subject: null, collection: {...}}` 原样传回前端。

**修复策略升级**：collection_repo 改为 `continue` 跳过整条记录（与 subject_repo 一致），确保脏数据完全不进入 API 响应。

## 文件变动（v2 增量）

| 文件 | 操作 | 说明 |
|------|------|------|
| `backend/app/repositories/collection_repo.py` | 修改 | `_sanitize_subject` → `_is_valid_subject`；4 处调用从 `subject=None` 改为 `continue` 跳过 |

## 测试验证

```powershell
cd e:\HACCI\Documents\tools\OtakuNeko\backend
uv run uvicorn app.main:app --reload
```

访问 `GET /api/v1/collections/` 不应再出现空卡片。

## 后续待办

---

## 任务简述
修复 `GET /api/v1/collections/` 500 错误：数据库中 Subject 记录的 `name` 字段为空字符串，导致 `CollectionWithSubject` Pydantic 模型校验失败（`String should have at least 1 character`）。

## 根因分析
- `SubjectRead.name` 字段定义了 `min_length=1`
- `CollectionWithSubject.subject` 虽然是 `Optional[SubjectRead]`（允许 `None`），但 LEFT OUTER JOIN 返回的 subject ORM 对象不为 `None` 而 `name` 为空串时，Pydantic 完整校验触发
- 类似问题也存在于 `SubjectWithCollection`（subject 为必填），会导致连锁崩溃

## 核心文件变动

| 文件 | 操作 | 说明 |
|------|------|------|
| `backend/app/repositories/collection_repo.py` | 修改 | 新增 `_is_valid_subject` 静态方法；4 处 `CollectionWithSubject` 构造点均通过 `continue` 跳过 name 为空的记录 |
| `backend/app/repositories/subject_repo.py` | 修改 | 新增 `_is_valid_subject` 静态方法；3 处 `SubjectWithCollection` 构造点（`get_by_source`、`search_by_name`、`get_all`）通过该方法跳过 name 为空的记录 |

## 修复策略
- **collection_repo**：`CollectionWithSubject.subject` 为 Optional，但 name 空时直接 `continue` 跳过整条记录
- **subject_repo**：`SubjectWithCollection.subject` 非 Optional → name 空时 `continue` 跳过整行

## 测试验证

```powershell
# 在宿主环境重启后端服务验证
cd e:\HACCI\Documents\tools\OtakuNeko\backend
uv run uvicorn app.main:app --reload
```

**建议**：同步清理数据库中 `name` 为空的 Subject 脏数据：
```sql
-- 查看受影响记录
SELECT id, source, source_id, name FROM subject WHERE name = '' OR name IS NULL;
-- 谨慎执行删除
-- DELETE FROM subject WHERE name = '' OR name IS NULL;
```

## 后续待办
- P1-02：密码强度校验
- P1-03：日志敏感信息脱敏
- 数据库 Subject 表脏数据清理（name 为空记录）
