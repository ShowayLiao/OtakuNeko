# Disable Nine-Grid Import Interface in SettingsModal

## 1. Problem Analysis
The nine-grid import interface in SettingsModal.tsx consists of two main parts:
- A button labeled "九宫格导入基础信息" (Nine-grid import basic information) that triggers the modal
- The `GridImportModal` component that renders the actual nine-grid import UI

## 2. Implementation Plan
To disable the nine-grid import interface:

### 2.1 Remove the Trigger Button
- Locate the button element that opens the nine-grid import modal (lines 424-427)
- Remove this button and its surrounding container

### 2.2 Remove the Modal Component
- Locate the `GridImportModal` component rendering (line 482)
- Remove this component from the render output

### 2.3 Remove Related State and Imports
- Remove the `isGridImportOpen` state (line 34)
- Remove the `GridImportModal` import (line 12)

## 3. Code Changes

### 3.1 Remove Import
```diff
- import { GridImportModal } from './GridImportModal';
```

### 3.2 Remove State
```diff
- const [isGridImportOpen, setIsGridImportOpen] = useState(false);
```

### 3.3 Remove Trigger Button
```diff
- {noBangumiAccount && (
-   <div className="space-y-2">
-     <Button variant="outline" size="sm" onClick={() => setIsGridImportOpen(true)} className="flex items-center gap-2 w-full">
-       <Grid3x3 className="w-4 h-4" />
-       九宫格导入基础信息
-     </Button>
-     <p className="text-xs text-gray-500 dark:text-gray-400">通过填写动画喜好宫格，快速生成你的二次元画像</p>
-   </div>
- )}
```

### 3.4 Remove Modal Component
```diff
- <GridImportModal isOpen={isGridImportOpen} onClose={() => setIsGridImportOpen(false)} />
```

## 4. Verification
- The nine-grid import button will no longer appear in the SettingsModal
- The GridImportModal component will not be rendered
- All other settings features will remain fully operational
- The SettingsModal will continue to function normally

This change will effectively disable the nine-grid import interface while maintaining all other functionality in the SettingsModal component.