## 实现计划

### 1. 问题分析
- 后端API已经合并为统一的 `POST /collections/` 接口，使用查询参数 `sid` 来区分更新和创建操作
- 前端当前实现仍在使用旧的 `PUT /collections/{subjectId}` 和 `POST /collections/` 两个接口
- 需要修改前端代码，使其调用统一的 `POST /collections/` 接口，并正确传递查询参数 `sid`

### 2. 修改方案
- **修改 `updateOrAddCollection` 函数**：将所有请求都改为调用 `POST /collections/` 端点
- **添加查询参数**：将 `subjectId` 作为查询参数 `sid` 传递
- **保持请求体格式**：请求体仍使用 `{ collection: collectionData, subject: subjectData }` 格式

### 3. 具体修改点
- **文件**：`e:\HACCI\Documents\tools\OtakuNeko\frontend\src\lib\api.ts`
- **函数**：`updateOrAddCollection`
- **修改内容**：
  - 删除条件判断，统一使用 `api.post` 调用
  - 将 `subjectId` 作为查询参数 `sid` 传递
  - 保持请求体结构不变

### 4. 验证要点
- 验证更新现有收藏功能正常
- 验证创建新收藏功能正常
- 验证错误处理机制正常
- 验证数据在前端界面的展示精度

### 5. 预期效果
- 前端代码与后端API完全对齐
- 所有收藏操作都通过统一的 `POST /collections/` 接口完成
- 正确处理查询参数 `sid`
- 保持原有功能不变

### 6. 技术细节
- 使用 `axios` 的 `params` 选项传递查询参数
- 保持原函数签名不变，确保调用处无需修改
- 保留完整的错误处理机制